/** localStorage key: signed token from hawola-admin for bypassing under-construction on the storefront */
export const STOREFRONT_PREVIEW_LS_KEY = "hawola_storefront_preview_token";

/** Query param when opening the storefront from admin (stored to LS then stripped from URL) */
export const STOREFRONT_PREVIEW_URL_PARAM = "storefront_preview";
